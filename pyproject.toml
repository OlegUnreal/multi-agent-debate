[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "multi-agent-debate"
version = "0.3.0"
description = "Multi-agent debate system: proposer, critic, judge with isolated memory + web UI."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.40",
    "python-dotenv>=1.0",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
debate = "debate.__main__:main"
debate-ui = "debate.server:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
